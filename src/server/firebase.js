import firebase from 'firebase';

var firebaseConfig = {
    //configDeatils of your firebase project
  };
  // Initialize Firebase
  firebase.initializeApp(firebaseConfig);

  export default firebase;